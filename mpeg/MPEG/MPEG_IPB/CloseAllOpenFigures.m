function CloseAllOpenFigures(handles)
if (isfield(handles,'Fig1'))
    if (ishandle(handles.Fig1));
        close(handles.Fig1);
    end    
end
if (isfield(handles,'Fig2'))
    if (ishandle(handles.Fig2));
        close(handles.Fig2);
    end    
end
if (isfield(handles,'Fig3'))
    if (ishandle(handles.Fig3));
        close(handles.Fig3);
    end    
end
if (isfield(handles,'Fig4'))
    if (ishandle(handles.Fig4));
        close(handles.Fig4);
    end    
end
if (isfield(handles,'Fig5'))
    if (ishandle(handles.Fig5));
        close(handles.Fig5);
    end    
end
if (isfield(handles,'Fig6'))
    if (ishandle(handles.Fig6));
        close(handles.Fig6);
    end    
end
if (isfield(handles,'Fig7'))
    if (ishandle(handles.Fig7));
        close(handles.Fig7);
    end    
end
if (isfield(handles,'Fig8'))
    if (ishandle(handles.Fig8));
        close(handles.Fig8);
    end    
end
if (isfield(handles,'Fig9'))
    if (ishandle(handles.Fig9));
        close(handles.Fig9);
    end    
end
if (isfield(handles,'Fig10'))
    if (ishandle(handles.Fig10));
        close(handles.Fig10);
    end    
end